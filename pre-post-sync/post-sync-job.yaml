apiVersion: batch/v1
kind: Job
metadata:
  name: after
  annotations:
    argocd.argoproj.io/hook: PostSync
    argocd.argoproj.io/hook-delete-policy: HookSucceeded
spec:
  template:
    spec:
      containers:
      - name: echo
        image: alpine:latest
        command: ["echo", "test string for post sync"]
      restartPolicy: Never
  backoffLimit: 0
